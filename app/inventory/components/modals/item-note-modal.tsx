"use client";
import { ModalShell } from "../modal-shell";
export function ItemNoteModal({ note,error,saving,onChange,onSave,onClose }: { note:string; error:string|null; saving:boolean; onChange:(v:string)=>void; onSave:()=>void; onClose:()=>void }) { return <ModalShell title="Add Inventory Note" onClose={onClose} footer={<div className="flex justify-end gap-2"><button onClick={onClose} className="h-10 border border-border px-4 text-xs font-black">Cancel</button><button disabled={saving} onClick={onSave} className="h-10 bg-primary px-4 text-xs font-black text-primary-foreground">{saving?"Saving…":"Add Note"}</button></div>}><textarea autoFocus className="min-h-40 w-full border border-border bg-background p-3 text-sm" value={note} onChange={e=>onChange(e.target.value)} placeholder="Internal inventory note…"/>{error&&<div className="mt-4 border border-rose-500/40 bg-rose-500/10 p-3 text-xs font-semibold text-rose-600 dark:text-rose-300">{error}</div>}</ModalShell>; }
