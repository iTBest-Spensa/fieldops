"use client";
import type { DbProfile, DbSite, LocationForm } from "../../types";
import { ModalShell } from "../modal-shell";
const input="h-10 w-full border border-border bg-background px-3 text-sm";
export function LocationFormModal({ form, sites, profiles, error, saving, onChange, onSave, onClose }: { form:LocationForm; sites:DbSite[]; profiles:DbProfile[]; error:string|null; saving:boolean; onChange:(f:LocationForm)=>void; onSave:()=>void; onClose:()=>void }) { return <ModalShell title="New Inventory Location" subtitle="Warehouse, office, vehicle, technician stock or another controlled location." onClose={onClose} footer={<div className="flex justify-end gap-2"><button onClick={onClose} className="h-10 border border-border px-4 text-xs font-black">Cancel</button><button disabled={saving} onClick={onSave} className="h-10 bg-primary px-4 text-xs font-black text-primary-foreground">{saving?"Saving…":"Create Location"}</button></div>}><div className="grid gap-4 md:grid-cols-2"><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Location Name</span><input className={input} value={form.name} onChange={e=>onChange({...form,name:e.target.value})}/></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Code</span><input className={input} value={form.code} onChange={e=>onChange({...form,code:e.target.value})}/></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Type</span><select className={input} value={form.locationType} onChange={e=>onChange({...form,locationType:e.target.value})}><option value="warehouse">Warehouse</option><option value="office">Office</option><option value="vehicle">Vehicle</option><option value="technician">Technician</option><option value="other">Other</option></select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Site</span><select className={input} value={form.siteId} onChange={e=>onChange({...form,siteId:e.target.value})}><option value="">None</option>{sites.filter(s=>s.active!==false).map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Technician</span><select className={input} value={form.technicianId} onChange={e=>onChange({...form,technicianId:e.target.value})}><option value="">None</option>{profiles.filter(p=>p.active).map(p=><option key={p.id} value={p.id}>{p.full_name||p.email}</option>)}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Vehicle Identifier</span><input className={input} value={form.vehicleIdentifier} onChange={e=>onChange({...form,vehicleIdentifier:e.target.value})}/></label></div><label className="mt-4 block"><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Notes</span><textarea className="min-h-20 w-full border border-border bg-background p-3 text-sm" value={form.notes} onChange={e=>onChange({...form,notes:e.target.value})}/></label>{error&&<div className="mt-4 border border-rose-500/40 bg-rose-500/10 p-3 text-xs font-semibold text-rose-600 dark:text-rose-300">{error}</div>}</ModalShell>; }
