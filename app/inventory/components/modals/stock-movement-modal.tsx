"use client";
import type { DbInventoryItem, DbInventoryLocation, DbProfile, DbWorkOrder, MovementForm } from "../../types";
import { ModalShell } from "../modal-shell";
const input="h-10 w-full border border-border bg-background px-3 text-sm";
export function StockMovementModal({ form, items, locations, profiles, workOrders, canManage, error, saving, onChange, onSave, onClose }: { form:MovementForm; items:DbInventoryItem[]; locations:DbInventoryLocation[]; profiles:DbProfile[]; workOrders:DbWorkOrder[]; canManage:boolean; error:string|null; saving:boolean; onChange:(f:MovementForm)=>void; onSave:()=>void; onClose:()=>void }) { const needsTo=form.movementType==="transfer"; return <ModalShell title="Record Stock Movement" subtitle="Use Consume when the material was actually used on a work order; it also creates material usage for later billing." onClose={onClose} footer={<div className="flex justify-end gap-2"><button onClick={onClose} className="h-10 border border-border px-4 text-xs font-black">Cancel</button><button disabled={saving} onClick={onSave} className="h-10 bg-primary px-4 text-xs font-black text-primary-foreground">{saving?"Posting…":"Post Movement"}</button></div>}><div className="grid gap-4 md:grid-cols-2"><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Movement</span><select className={input} value={form.movementType} onChange={e=>onChange({...form,movementType:e.target.value as MovementForm["movementType"]})}>{canManage&&<option value="receive">Manual Receive</option>}<option value="issue">Issue</option><option value="consume">Consume on Work Order</option>{canManage&&<option value="transfer">Transfer</option>}{canManage&&<option value="adjustment">Adjustment (+/-)</option>}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Item</span><select className={input} value={form.itemId} onChange={e=>onChange({...form,itemId:e.target.value})}><option value="">Choose item</option>{items.filter(i=>i.active).map(i=><option key={i.id} value={i.id}>{i.name}{i.sku?` · ${i.sku}`:""}</option>)}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">{needsTo?"From Location":"Location"}</span><select className={input} value={form.locationId} onChange={e=>onChange({...form,locationId:e.target.value})}><option value="">Choose location</option>{locations.filter(l=>l.active).map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label>{needsTo&&<label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">To Location</span><select className={input} value={form.toLocationId} onChange={e=>onChange({...form,toLocationId:e.target.value})}><option value="">Choose location</option>{locations.filter(l=>l.active).map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label>}<label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Quantity {form.movementType==="adjustment"?"(+/-)":""}</span><input type="number" step="0.01" className={input} value={form.quantity} onChange={e=>onChange({...form,quantity:e.target.value})}/></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Unit Cost</span><input type="number" min="0" step="0.01" className={input} value={form.unitCost} onChange={e=>onChange({...form,unitCost:e.target.value})}/></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Work Order</span><select className={input} value={form.workOrderId} onChange={e=>onChange({...form,workOrderId:e.target.value})}><option value="">None</option>{workOrders.filter(w=>!['closed','cancelled'].includes(w.status)).map(w=><option key={w.id} value={w.id}>{w.work_order_number} · {w.title}</option>)}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Technician</span><select className={input} value={form.technicianId} onChange={e=>onChange({...form,technicianId:e.target.value})}><option value="">None</option>{profiles.filter(p=>p.active).map(p=><option key={p.id} value={p.id}>{p.full_name||p.email}</option>)}</select></label><label><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Reference</span><input className={input} value={form.reference} onChange={e=>onChange({...form,reference:e.target.value})}/></label></div><label className="mt-4 block"><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Notes</span><textarea className="min-h-20 w-full border border-border bg-background p-3 text-sm" value={form.notes} onChange={e=>onChange({...form,notes:e.target.value})}/></label>{error&&<div className="mt-4 border border-rose-500/40 bg-rose-500/10 p-3 text-xs font-semibold text-rose-600 dark:text-rose-300">{error}</div>}</ModalShell>; }
