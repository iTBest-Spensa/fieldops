"use client";
import type { DbInventoryLocation } from "../../types";
import { ModalShell } from "../modal-shell";
const input="h-10 w-full border border-border bg-background px-3 text-sm";
export function ReconciliationStartModal({ locationId, notes, locations, error, saving, onLocationChange, onNotesChange, onSave, onClose }: { locationId:string; notes:string; locations:DbInventoryLocation[]; error:string|null; saving:boolean; onLocationChange:(v:string)=>void; onNotesChange:(v:string)=>void; onSave:()=>void; onClose:()=>void }) { return <ModalShell title="Start Reconciliation" subtitle="The system snapshots stock for the selected location. Posting is blocked if stock moves after the count starts." onClose={onClose} footer={<div className="flex justify-end gap-2"><button onClick={onClose} className="h-10 border border-border px-4 text-xs font-black">Cancel</button><button disabled={saving} onClick={onSave} className="h-10 bg-primary px-4 text-xs font-black text-primary-foreground">{saving?"Starting…":"Start Count"}</button></div>}><label className="block"><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Location</span><select className={input} value={locationId} onChange={e=>onLocationChange(e.target.value)}><option value="">Choose location</option>{locations.filter(l=>l.active).map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label><label className="mt-4 block"><span className="mb-1 block text-[10px] font-black uppercase text-muted-foreground">Count Notes</span><textarea className="min-h-24 w-full border border-border bg-background p-3 text-sm" value={notes} onChange={e=>onNotesChange(e.target.value)}/></label>{error&&<div className="mt-4 border border-rose-500/40 bg-rose-500/10 p-3 text-xs font-semibold text-rose-600 dark:text-rose-300">{error}</div>}</ModalShell>; }
