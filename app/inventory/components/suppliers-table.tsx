"use client";

import { Pencil } from "lucide-react";
import type { DbSupplier } from "../types";
export function SuppliersTable({ suppliers, onEdit }: { suppliers: DbSupplier[]; onEdit: (supplier: DbSupplier) => void }) {
  if (!suppliers.length) return <div className="border border-border bg-card p-8 text-center text-sm text-muted-foreground">No suppliers yet.</div>;
  return <div className="overflow-x-auto border border-border bg-card"><table className="w-full min-w-[900px] text-left"><thead className="bg-muted/40 text-[10px] font-black uppercase text-muted-foreground"><tr><th className="px-4 py-3">Supplier</th><th className="px-4 py-3">Contact</th><th className="px-4 py-3">Phone</th><th className="px-4 py-3">Location</th><th className="px-4 py-3">Terms</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Edit</th></tr></thead><tbody>{suppliers.map(s=><tr key={s.id} className="border-t border-border"><td className="px-4 py-4"><div className="text-sm font-black">{s.name}</div><div className="text-[10px] text-muted-foreground">{s.supplier_number}</div></td><td className="px-4 py-4 text-xs"><div className="font-bold">{s.contact_name||"—"}</div><div className="text-muted-foreground">{s.email||""}</div></td><td className="px-4 py-4 text-xs">{s.phone||"—"}</td><td className="px-4 py-4 text-xs">{[s.city,s.province_state].filter(Boolean).join(", ")||"—"}</td><td className="px-4 py-4 text-xs">{s.payment_terms_days} days</td><td className="px-4 py-4 text-xs font-black uppercase">{s.active?"Active":"Inactive"}</td><td className="px-4 py-4 text-right"><button onClick={()=>onEdit(s)} className="inline-flex items-center gap-1 text-xs font-black text-primary"><Pencil className="h-3.5 w-3.5"/> Edit</button></td></tr>)}</tbody></table></div>;
}
