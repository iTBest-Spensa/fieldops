"use client";
import { Banknote, CreditCard } from "lucide-react";
import type { SettingsForm } from "../types";
import { Field, SectionCard, inputClass, textareaClass } from "./section-card";
export function BillingPanel({ form, disabled, saving, onChange, onSave }: { form:SettingsForm; disabled:boolean; saving:boolean; onChange:(form:SettingsForm)=>void; onSave:()=>void }) {
  return <div className="space-y-5"><SectionCard title="Billing defaults" description="Defaults for future invoices. Customer-specific terms and invoice-specific edits can still override them."><div className="grid gap-4 md:grid-cols-2"><Field label="Default payment terms (days)"><input disabled={disabled} inputMode="numeric" className={inputClass} value={form.defaultPaymentTermsDays} onChange={e=>onChange({...form,defaultPaymentTermsDays:e.target.value})}/></Field><Field label="Default tax rate (%)"><input disabled={disabled} inputMode="decimal" className={inputClass} value={form.defaultTaxRatePercent} onChange={e=>onChange({...form,defaultTaxRatePercent:e.target.value})}/></Field><div className="md:col-span-2"><Field label="Invoice footer"><textarea disabled={disabled} className={textareaClass} value={form.invoiceFooter} onChange={e=>onChange({...form,invoiceFooter:e.target.value})} placeholder="Payment instructions, thank-you note, legal text…"/></Field></div></div></SectionCard><SectionCard title="Payment methods" description="FieldOps customer-facing payment terminology remains Cash and Tap."><div className="grid gap-3 sm:grid-cols-2"><div className="flex items-center gap-3 border border-border p-4"><Banknote className="h-5 w-5 text-primary"/><div><div className="font-black">Cash</div><div className="text-xs text-muted-foreground">Physical cash payment</div></div></div><div className="flex items-center gap-3 border border-border p-4"><CreditCard className="h-5 w-5 text-primary"/><div><div className="font-black">Tap</div><div className="text-xs text-muted-foreground">Card / terminal payment</div></div></div></div></SectionCard><div className="flex justify-end"><button disabled={disabled||saving} onClick={onSave} className="h-11 bg-primary px-5 text-sm font-black text-primary-foreground disabled:opacity-50">{saving?"Saving…":"Save Billing Settings"}</button></div></div>;
}
